{
  "name": "editorjs-ecad-viewer-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:no-check -w ecad-viewer && npm run build -w @editorjs/ecad-viewer",
    "build_dist_copy": "node scripts/build_dist_copy.mjs",
    "build:all": "npm run build && npm run build_dist_copy"
  }
}
